import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { Box } from '../index';

<Meta title="Components/Box" component={Box} />
export const Template = (args) => <Box {...args} />;

<Canvas>
  <Story name="Default" args={{ children: 'I am a box' }}>
    {Template.bind({})}
  </Story>
</Canvas>
