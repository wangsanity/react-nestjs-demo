import React from 'react';
export interface AuthenticationProps {
    children: React.ReactElement;
}
export declare const Authentication: ({ children }: AuthenticationProps) => React.ReactElement;
